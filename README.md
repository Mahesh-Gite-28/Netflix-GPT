#Neflix GPT

-create react app
-configured tailwindcss
-plan the features
-Routing (Login,Browse through body [uses router])
-start building login page
    -header 
    -sign up /Sign In form
        -validation inside form
        -firebase setup
        -use firebase for authentication
        -firebase deploy 
        -create signup user account
        -make the redux store 

#Features

-Login/Sign Up
    -Sign In/Sign up form 
    -redirect to Browse Page
    
-Browse(after authentication)
     -Header
     -Main Movie
          -Trailer in Background
          -Title & Description 
          -Movie Suggestions
                -MovieLists * N
        
-NetflixGPT
     -Search Bar
     -Movie Suggestions


key Concepts learn

useref and ref={refvar_name} for input box
firebase deploy